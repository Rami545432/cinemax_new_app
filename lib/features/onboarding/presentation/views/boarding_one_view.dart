import 'package:cinemax_app_new/features/onboarding/presentation/views/boarding_one_view_body.dart';
import 'package:flutter/material.dart';

class BoardingOneView extends StatelessWidget {
  const BoardingOneView({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: BoardingOneViewBody());
}
