// import 'package:movify/shared/domain/auth_event.dart';

// abstract interface class AuthEventSink {
//   /// Push a new auth event to all subscribers.
//   void addEvent(AuthEvent event);

//   /// Stream of auth events — subscribers listen here.
//   Stream<AuthEvent> get authEventStream;

//   /// Synchronous access to the last emitted event.
//   /// Equivalent to BehaviorSubject.value — new subscribers get this immediately.
//   AuthEvent get currentEvent;
// }
